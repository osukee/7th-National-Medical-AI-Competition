# PB 0.440 → 0.48 戦略レビュー（6h GPU制約）

## 1. 現状整理（`kaggle/train_notebook.py` + `experiments`）

- 現在の主設定は `image_size=512`, `in_channels=1`, `epochs=20`, `batch_size=4`, `loss_type=optimized`, `encoder=efficientnet-b5`, `architecture=unetplusplus`, `tta_mode=dihedral8`, `n_folds_ensemble=3`。
- 実験ログ上の改善実績:
  - `exp_016` encoder b4: **+0.011**（0.407→0.419）
  - `exp_017` optimized loss: **+0.0099**（0.419→0.4289）
  - `exp_019` U-Net++: **+0.0018**（0.4289→0.4307）
  - `exp_023` geometric augmentation: **+0.010**（0.4307→0.44039）
  - `auto_iterations.json`: best **0.44049**（改善名: `resolution_576`）
- `exp_025` の知見: 推論で弱foldを混ぜるとノイズ増。**top-3 fold重み付け**を推奨。

## 2. 候補7施策の評価（優先度順）

### A. **SWA**（優先度: 高）
- 期待効果: **+0.003〜0.008**
- 工数/リスク: 低（実装小、学習終盤のみ）
- 理由: 現状はCosineAnnealingで最終重みを1点採用。SWAで「終盤の重み平均」を取るとSSIM系が安定しやすい。
- 6h適合: 良い（追加は終盤数epochのみ）

### B. **Progressive Training**（優先度: 高）
- 期待効果: **+0.004〜0.010**
- 工数/リスク: 中（学習スケジュール調整）
- 理由: `auto_iterations` で `resolution_576` が最高値（0.44049）。  
  ただし全epochを576で回すと時間超過しやすいので、**前半512→後半576**が現実的。
- 6h適合: 良い（後半のみ高解像度）

### C. **3ch入力**（優先度: 中）
- 期待効果: **+0.002〜0.008**（当たれば大きいが不確実）
- 工数/リスク: 中（Dataset変更、前処理設計が重要）
- 推奨構成: `ch0=raw(gray), ch1=CLAHE, ch2=Sobel/Laplacian`。  
  単純RGB複製は効果が薄い可能性。
- 6h適合: まず1-fold短縮検証が必要。

### D. **Encoder変更**（優先度: 中〜低）
- 期待効果: **-0.003〜+0.006**
- 工数/リスク: 中〜高（VRAM/時間増）
- 理由: b4→b5は既に反映済み。次のb6系は重く、6h制限で探索回数が減る。  
  まずはb5固定で他施策を優先した方が期待値が高い。

### E. **Perceptual Loss**（優先度: 低）
- 期待効果: **0〜+0.004**（不確実）
- 工数/リスク: 高（VGG特徴抽出で重い、医療グレースケールと特徴空間ミスマッチ）
- 理由: `src/model_improvements.py` に実装はあるが、コンペ本線で実証なし。  
  6h制限下では探索効率が悪い。

### F. **Deep Supervision**（優先度: 低）
- 期待効果: **0〜+0.004**
- 工数/リスク: 高（モデル出力/損失/推論の改修範囲が広い）
- 理由: 現行コードに統合されておらず、実装リスクの割に即効性が低い。

### G. **全5Fold（推論で全fold使用）**（優先度: 非推奨）
- 期待効果: **0〜マイナス**
- 工数/リスク: 低
- 理由: `exp_025` の仮説と現行設定は「弱fold混入で構造ノイズ増」。  
  現時点では **top-3維持** が妥当。

## 3. 6時間での実行戦略（推奨）

### フェーズ1（最優先、まず通す）
- **SWA + Progressive(512→576)** を同時導入（b5, top-3 fold維持）
- 目標: **0.448〜0.458**

### フェーズ2（次点）
- 上記に **3ch入力** を追加して再学習（同じCV設定）
- 目標: **+0.003〜0.008** 上積み（合計0.455〜0.466帯）

### フェーズ3（時間が残る場合のみ）
- Encoderの軽微探索（b5派生設定）  
- Perceptual / Deep Supervision / 全5foldはこのラウンドでは後回し

## 4. 0.48到達可能性の現実評価

- 現在best 0.44049 から 0.48 は **+0.0395** 必要。  
- 6h単発での到達はかなり厳しく、**複数ラウンド前提**が現実的。  
- ただし、SWA + Progressive + 3ch の組み合わせは、この候補群の中で最も期待値が高い。

## 5. 結論（採用順）

1. **採用**: SWA  
2. **採用**: Progressive Training（512→576）  
3. **条件付き採用**: 3ch入力（1-fold短縮検証で有望なら本番）  
4. **保留**: Encoder変更  
5. **非優先**: Perceptual Loss, Deep Supervision  
6. **非推奨**: 推論で全5Fold使用

