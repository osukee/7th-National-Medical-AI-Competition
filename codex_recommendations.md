# Codex Recommendations (SSIM-focused, 6h Kaggle GPU limit)

> Generated by Codex CLI v0.101.0 (gpt-5.3-codex) on 2026-02-14

## Summary

| # | Decision | Rationale |
|---|----------|-----------|
| 1 | **Encoder: b4** (not b5) | b4 is validated (exp_023: 0.44039). b5 is unvalidated + slower |
| 2 | **Epochs: 15** | 25 epochs too risky for 6h. 15 matches validated exp_023 |
| 3 | **Pseudo-labeling: OFF** | Unvalidated, large time overhead, weak batch-mean weighting |
| 4 | **Loss: `optimized`** (not `edge_weighted`) | `edge_weighted` was fragile in exp_020 |
| 5 | **Ensemble: top-3** (not 5) | Weak folds add noise (exp_025 rationale) |
| 6 | **TTA: `flip4`** (not `dihedral8`) | Save runtime for 6h limit |

## Codex's Recommended Config

```python
encoder = "efficientnet-b4"
architecture = "unetplusplus"
loss_type = "optimized"
epochs = 15
batch_size = 4
learning_rate = 1e-4
n_folds_ensemble = 3
fold_rank_weights = [1.0, 0.7, 0.4]
tta_enabled = True
tta_mode = "flip4"
tta_aggregate = "median"
pseudo_label_enabled = False
augmentation_enabled = True
augmentation_mode = "geometric"
mean_matching_enabled = False
```

## Key Code References

- `edge_weighted` loss historically hurt LB: `exp_020`
- Top-3 rationale: `exp_025` fold selection logs
- Pseudo-label batch-mean weighting issue: line 2954-2955
