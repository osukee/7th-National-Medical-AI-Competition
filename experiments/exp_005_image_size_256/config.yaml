experiment:
  id: exp_005_image_size_256
  created_at: 2026-01-06

model:
  architecture: unet
  encoder: resnet34
  encoder_weights: imagenet

training:
  epochs: 3
  batch_size: 8
  learning_rate: 0.0001

data:
  augmentation: false
  train_split: 0.8
  image_size: 256  # 512→256に縮小

loss:
  l1_weight: 1.0
  ssim_weight: 1.0
